/* ABOUTME: Operator configuration for Solanum IRCd
/* ABOUTME: Tailscale users get admin access with shared password

/* Any Tailscale connection can oper with: /OPER admin <password> */
operator "admin" {
    user = "*@*.${TAILSCALE_DOMAIN}", "*@100.64.0.0/10";
    password = "${OPERATOR_PASSWORD}";
    privset = "admin";
    snomask = "+Zbfkrsuy";
};

