# ABOUTME: Convos IRC web client for MagNET
# ABOUTME: Persistent IRC bouncer with modern web UI, connects via Fly.io internal network

FROM ghcr.io/convos-chat/convos:stable

ENV CONVOS_HOME=/data

EXPOSE 3000
