/* ABOUTME: Operator configuration for Solanum IRCd
/* ABOUTME: Tailscale users get admin access - Tailscale ACLs provide authentication

/* Any connection to the Tailscale listener can oper with: /OPER admin <password>
 * This is safe because only Tailscale users can reach the Tailscale interface.
 * Password is OPERATOR_PASSWORD from Fly.io secrets.
 */
operator "admin" {
    user = "*@*";
    password = "${OPER_PASSWORD_HASH}";
    privset = "admin";
    snomask = "+Zbfkrsuy";
};

